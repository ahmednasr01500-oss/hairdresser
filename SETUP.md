# خطوات التشغيل — تطبيق أخضر 🌿

الكود كله جاهز وشغّال. الخطوات دي كلها في **Firebase Console** ومحتاجة حساب
صاحب المشروع — أنا مقدرش أعملها من هنا. كل خطوة ٥ دقايق بالكتير.

الرابط: <https://console.firebase.google.com/project/akhdar-89577>

> **ليه مشروع Firebase لوحده؟** الإعدادات الأولى كانت لمشروع
> `hairdresser-d6d24` — وطلع فيه تطبيق صالون تجميل شغّال وبياناته بتتكتب
> يوميًا. ومسارات `settings` و`categories` و`customers` مشتركة بين
> التطبيقين، فكان أخضر هيقرأ أقسام الصالون ("صبغات وألوان الشعر") كأنها
> أقسام خضار، واستيراد `seed.json` كان هيمسح بيانات الصالون بالكامل.
> عشان كده أخضر بقى على مشروعه لوحده: حصة الباقة المجانية كلها ليه،
> وقواعد الأمان بسيطة، ولو حصل غلط مايأثرش على المشروع التاني.

---

## (١) فعّل تسجيل الدخول

**Build → Authentication → Get started**، وبعدين من تبويب **Sign-in method**:

| المزوّد | ليه؟ |
|---|---|
| **Anonymous** | العميل بياخد `uid` ثابت من غير كلمة سر — ده اللي بيربط الطلبات بصاحبها ويمنع أي حد يشوف طلبات غيره. |
| **Email/Password** | حساب صاحب المحل اللي بيدخل بيه لوحة التحكم. |

بعد ما تفعّل Email/Password، من تبويب **Users → Add user** اعمل حساب اللوحة
(مثلًا `admin@akhdar.com` وكلمة سر قوية) — **واكتب الـ User UID بتاعه**، هتحتاجه
في خطوة (٤).

> **مهم:** ما فعّلناش دخول برقم التليفون بكود تحقق (OTP)، لأنه محتاج تفعيل الفوترة
> (خطة Blaze) وبيتكلّف على كل رسالة. العميل بيدخل باسمه ورقمه بس.

---

## (٢) سجّل تطبيق أندرويد و iOS (اختياري بس مستحسن)

التطبيق شغّال دلوقتي بإعدادات الويب اللي بعتّها، والديتابيز والمصادقة
بيشتغلوا بيها عادي. لكن الأنضف إنك تسجّل كل منصة لوحدها:

**Project settings → Your apps → Add app**

| المنصة | الـ ID المطلوب |
|---|---|
| Android | `com.akhdar.akhdar` |
| iOS | `com.akhdar.akhdar` |

بعد كده هات الـ **App ID** بتاع كل واحد وحطه في
[`lib/firebase_options.dart`](lib/firebase_options.dart) — في `android` و`ios`،
مكان `_webAppId`. الباقي زي ما هو.

---

## (٣) حط اللوجو

احفظ ملف اللوجو (الصورة الشفافة PNG) باسم:

```
assets/images/logo.png
```

من غيره التطبيق بيرسم لوجو بديل بسيط — شغّال بس مش الهوية الحقيقية.

لو عايز أيقونة التطبيق نفسها (اللي بتظهر على شاشة الموبايل) تبقى اللوجو كمان،
قوللي وأنا أضيف حزمة `flutter_launcher_icons` وأولّدها لكل المقاسات.

---

## (٤) ارفع قواعد الأمان

**Realtime Database → Rules**، الزق محتوى الملف
[`firebase/database.rules.json`](firebase/database.rules.json) كله واضغط **Publish**.

القواعد دي بتعمل إيه:

- الكتالوج (الأصناف والأقسام والإعدادات) **مقروء للكل** — عشان العميل يتفرّج قبل ما يسجّل.
- الكتابة على الكتالوج **لصاحب المحل بس**.
- كل عميل يشوف **طلباته هو بس**، وصاحب المحل يشوف الكل.
- أكواد الخصم: العميل يقدر يتحقق من كود يعرفه، لكن ما يقدرش يجيب قايمة الأكواد كلها.

بعدها من **Realtime Database → Data** اعمل عقدة اسمها `admins` جواها
الـ UID اللي أخدته في خطوة (١) وقيمته `true`:

```json
{ "admins": { "الصق-الـ-UID-هنا": true } }
```

> من غير الخطوة دي لوحة التحكم هتدخل بس مش هتقدر تحفظ أي حاجة.

---

## (٥) حط بيانات البداية

**Realtime Database → Data → ⋮ → Import JSON** واختار
[`firebase/seed.json`](firebase/seed.json).

فيه أقسام وأصناف مبدئية عشان التطبيق ما يفتحش فاضي.
**الأسعار اللي فيه أرقام تجريبية — غيّرها من لوحة التحكم بأسعار المحل الحقيقية.**

> ⚠️ الاستيراد بيمسح أي داتا موجودة. اعمله وإنت لسه في البداية بس.

---

## (٦) النشر — صفحة التحميل + لوحة التحكم

مرة واحدة بس على الجهاز:

```bash
npm install -g firebase-tools
```

وبعدين سجّل دخول بحساب جوجل بتاع المشروع:

```bash
firebase login
```

وبعد كده كل مرة عايز تنشر، أمر واحد:

```powershell
.\deploy.ps1
```

السكربت بيبني التطبيق، بينسخ الـ APK لصفحة التحميل، وينشر الموقع. النتيجة:

| الرابط | لمين |
|---|---|
| `https://akhdar-89577.web.app` | **صفحة التحميل** — ده اللي تبعته للزباين |
| `https://akhdar-89577.web.app/admin/` | **لوحة التحكم** — احفظه في المفضلة |

صفحة التحميل بتعرف نوع الجهاز لوحدها: الأندرويد بيشوف زرار التحميل مع خطوات
التثبيت، والآيفون بيشوف رسالة "قريبًا" مع أزرار الواتساب والتليفون.

> لو غيّرت في اللوحة أو الصفحة بس (من غير تعديل على التطبيق):
> `.\deploy.ps1 -SkipBuild` — أسرع بكتير.

**حط اللوجو كمان في `hosting/logo.png`** عشان يظهر في صفحة التحميل وفي
معاينة الرابط لما تبعته على واتساب. من غيره بتظهر أيقونة تفاحة مؤقتة.

---

## (٧) بناء التطبيق

### أندرويد — يتعمل من هنا على طول

`deploy.ps1` بيعمل ده لوحده. لو عايز تبني بس من غير نشر:

```bash
flutter build apk --release --split-per-abi
```

بيطلع ٣ ملفات في `build/app/outputs/flutter-apk/`. اللي بيتوزّع هو:

```
app-arm64-v8a-release.apk    ← ٢٤ ميجا
```

ده بيشتغل على كل موبايل اتباع في آخر ٧ سنين تقريبًا.

> لو حد عنده موبايل قديم أوي وقاله "التطبيق مش متوافق"، ابعتله
> `app-armeabi-v7a-release.apk` (٢٢ ميجا).
> ولو عايز ملف واحد يشتغل على كل حاجة، اعمل `flutter build apk --release`
> من غير `--split-per-abi` وابعت `app-release.apk` (٥٩ ميجا).

> النسخة دي موقّعة بمفتاح الديبج، وده كفاية للتوزيع المباشر.
> **قبل الرفع على Google Play** لازم مفتاح توقيع حقيقي — قوللي وأنا أظبّطه.

### آيفون — محتاج جهاز ماك

الكود متوافق مع iOS بالكامل والأذونات متظبّطة، بس **بناء ورفع نسخة iOS
مستحيل من ويندوز** — Apple بتفرض إن الـ build يتم على macOS بـ Xcode.
الاختيارات:

1. جهاز ماك (أو Mac mini) + حساب مطوّر Apple (٩٩ دولار/سنة).
2. خدمة بناء سحابية زي **Codemagic** — بتبني من غير ما تشتري ماك،
   بس لسه محتاج حساب Apple Developer عشان التوقيع.

على الماك قبل أول بناء:

```bash
cd ios && pod install
```

ولو ظهرت مشكلة في نسخة iOS، افتح `ios/Podfile` وخلي أول سطر:
`platform :ios, '15.0'` (متطلب حزم Firebase الحديثة).

#### Firebase App Distribution مش بديل عن ده

سؤال بيتكرر: نستخدم لينك من Firebase للآيفون؟ الإجابة **لأ** — الخدمة بتوزّع
ملف `.ipa` **مبني ومُوقّع أصلًا**، فهي بتيجي *بعد* الماك مش بدله. وكمان
للآيفون بتتطلب تسجيل رقم كل جهاز (UDID) يدويًا بحد أقصى ١٠٠ جهاز في السنة —
تنفع للتجربة، مش لتوزيع على زباين.

للأندرويد الخدمة شغّالة عادي من غير أي شروط، بس صفحة التحميل في خطوة (٦)
أبسط منها وكفاية.

---

## نقطة مؤجّلة بقرار: الإشعارات

دلوقتي العميل بيشوف حالة الطلب لما يفتح التطبيق (بتتحدّث لحظيًا وهو فاتح).
لكن **مفيش إشعار بيوصله والتطبيق مقفول**، وصاحب المحل مش بيوصله تنبيه
على الموبايل غير لما اللوحة مفتوحة.

ده محتاج Firebase Cloud Messaging، والجزء بتاع إرسال الإشعار محتاج
Cloud Functions اللي بتطلب **خطة Blaze**. لما الشغل يكبر ونحوّل الخطة، قوللي
وأنا أضيفه — التطبيق متبني بحيث الإضافة دي تبقى سهلة.
